<Window
    x:Class="Fortiva.AppHost.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fortiva.AppHost"
    Title="Fortiva">
    <Grid>
        <Rectangle Fill="{ThemeResource FortivaAmbientBrush}"/>
        <Ellipse Width="520" Height="520" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-120,-80,0"
                 Fill="{ThemeResource FortivaGlowOrbBrush}" IsHitTestVisible="False"/>
        <Ellipse Width="420" Height="420" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-60,0,0,-80"
                 Fill="{ThemeResource FortivaGlowOrbSecondaryBrush}" IsHitTestVisible="False"/>
        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom title bar area -->
        <Grid x:Name="AppTitleBar" Height="48" Background="{ThemeResource FortivaTitleBarBrush}"
              BorderBrush="{ThemeResource FortivaGlassBorderBrush}" BorderThickness="0,0,0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftPaddingColumn" Width="0"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition x:Name="RightPaddingColumn" Width="0"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" Margin="12,0" VerticalAlignment="Center">
                <Image x:Name="TitleBarLogo" Width="28" Height="28" Stretch="Uniform" VerticalAlignment="Center"/>
                <TextBlock x:Name="TitleText" FontWeight="SemiBold" FontSize="14"
                           Foreground="{ThemeResource FortivaHeadingBrush}" VerticalAlignment="Center"/>
            </StackPanel>
            <AutoSuggestBox x:Name="GlobalSearch" Grid.Column="2"
                            Style="{StaticResource FortivaSearchBox}"
                            PlaceholderText="Search vault…  (Ctrl+K · Ctrl+Shift+P commands)"
                            QueryIcon="Find"
                            MaxWidth="420" Margin="16,4,16,4"
                            VerticalAlignment="Center"
                            QuerySubmitted="GlobalSearch_QuerySubmitted"/>
            <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="8" Margin="8,0,12,0" VerticalAlignment="Center">
                <Border Style="{StaticResource FortivaStatusPill}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon x:Name="StatusIcon" Glyph="&#xE72E;" FontSize="11"
                                  VerticalAlignment="Center"/>
                        <TextBlock x:Name="StatusText" FontSize="11" MaxWidth="220"
                                   TextTrimming="CharacterEllipsis"
                                   Foreground="{ThemeResource FortivaBodyBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Button x:Name="LockBtn" Style="{StaticResource FortivaIconButton}"
                        ToolTipService.ToolTip="Lock vault"
                        Click="LockBtn_Click">
                    <FontIcon Glyph="&#xE72E;" FontSize="14"/>
                </Button>
                <Button x:Name="PanicBtn" Style="{StaticResource FortivaIconButton}"
                        ToolTipService.ToolTip="Panic lock — wipe clipboard and hide window"
                        Click="PanicBtn_Click">
                    <FontIcon x:Name="PanicIcon" Glyph="&#xE946;" FontSize="14"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- NavigationView -->
        <NavigationView x:Name="NavView"
                        Grid.Row="1"
                        Background="Transparent"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="false"
                        PaneDisplayMode="Left"
                        OpenPaneLength="220"
                        SelectionChanged="NavView_SelectionChanged">
            <NavigationView.Resources>
                <Style TargetType="NavigationViewItem" BasedOn="{StaticResource FortivaNavigationViewItem}"/>
            </NavigationView.Resources>
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="NavVault"   Tag="Vault"      Content="My Vault">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE8D7;"/></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="NavGenerator" Tag="Generator" Content="Password generator">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE9D5;"/></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="NavHealth"  Tag="Health"     Content="Security audit">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE946;"/></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="NavImport"  Tag="ImportExport" Content="Import / Export">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE8B5;"/></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="NavSettings" Tag="Settings"  Content="Settings">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE713;"/></NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItem x:Name="NavAudit"  Tag="Audit"  Content="Audit Log">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE81C;"/></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="NavAdmin"  Tag="Admin"  Content="Admin Console">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE8A1;"/></NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>
            <Frame x:Name="ContentFrame" Background="Transparent" IsNavigationStackEnabled="True"/>
        </NavigationView>
        </Grid>
    </Grid>
</Window>
